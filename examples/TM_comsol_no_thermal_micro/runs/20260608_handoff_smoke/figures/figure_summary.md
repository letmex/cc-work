# Figure Summary

No figures were generated by this one-step smoke training command.

Reason:

```text
The command only ran the minimal training path and produced diagnostics CSV plus a final NPZ field file. It did not run the plotting script.
```

Available non-figure outputs:

```text
tables/diagnostics_mixed_tm_summary.csv
tables/displacement_list.csv
artifacts/fields_mixed_tm_step_0000.npz
```

This absence of figures is acceptable for this workflow-only smoke package. No physical interpretation should be made from this run.
