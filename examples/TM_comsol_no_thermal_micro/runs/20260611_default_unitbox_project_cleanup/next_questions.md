# Next Questions

1. Should `plot_results.py` keep deprecated hidden `--corrected-*` aliases for
   one transition period, or should they be removed in the next cleanup pass?
2. Should historical result folders be migrated to the new output filenames, or
   should migration remain opt-in to avoid rewriting old evidence?
3. Is the automatic training-completion postprocess acceptable as the normal
   path for future full runs?

