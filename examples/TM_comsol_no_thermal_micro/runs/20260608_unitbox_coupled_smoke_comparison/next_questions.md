# Next Questions

1. Is the 12-step controlled comparison enough to accept `coord-normalization unit_box`
   for the next longer alpha-init history run?
2. Should the next run add a second seed before any full run?
3. Should reaction behavior be diagnosed in mechanics-only/coupled controlled
   mode before a longer full-stage run?

