# Next Questions

1. Should the next run be full D0020 with `coord_normalization=unit_box` and
   `alpha-init-intact`, preserving the same physical/model settings?
2. Should the full production run include default-alpha as a branch-selection
   comparison, or focus first on alpha-init-intact?
3. How many seeds are needed before treating the branch as robust rather than
   a single-seed observation?

