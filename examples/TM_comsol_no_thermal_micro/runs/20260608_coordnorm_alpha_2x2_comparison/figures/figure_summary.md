# Figure Summary

## `final_alpha_2x2.png`

- What it plots: final alpha field at step 11 for all four cases.
- Visual takeaway: `default_none` is nearly uniform/background, while both
  `unit_box` cases and `intact_none` show notch-localized amplification.
- Evidence status: supports a diagnostic branch-selection observation only.

## `final_He_current_2x2.png`

- What it plots: final `He_current` field at step 11 for all four cases.
- Visual takeaway: `default_none` has a broad/boundary-side drive pattern;
  `unit_box` cases concentrate the drive near the notch. `intact_none` is
  notch-amplified but weaker than `intact_unitbox`.
- Evidence status: supports a diagnostic observation about coordinate
  normalization; not physical validation.

## `final_mechanics_drive_2x2.png`

- What it plots: final mechanics-drive field used by the history mechanics
  branch at step 11.
- Visual takeaway: the mechanics-drive field follows the same branch contrast
  as `He_current`: raw/default is not notch-dominated, while `unit_box` cases
  are notch-dominated.
- Evidence status: diagnostic observation only.

## `reaction_comparison.png`

- What it plots: `reaction_N_tm_eff` versus `Delta` for all four cases.
- Visual takeaway: all signs remain positive over the 12-step subset.
  `unit_box` cases have lower reactions than raw-coordinate cases after damage
  localizes.
- Evidence status: diagnostic only; it does not validate physical stiffness.

