# Next Questions

1. Should the next task run the same full D0020 `alpha-init-intact + unit_box`
   route for another seed?
2. Should the next seed include the optional default-alpha branch, or only the
   primary alpha-init-intact branch?
3. What minimum seed count is required before treating the `unit_box`
   full-D0020 branch as robust enough for downstream interpretation?

