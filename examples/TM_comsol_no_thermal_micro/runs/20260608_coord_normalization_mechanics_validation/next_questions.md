# Next Questions

1. Should the next task run a short coupled alpha-init history smoke test with
   `--coord-normalization unit_box` and otherwise unchanged physical/model
   settings?
2. Should energy-continuation drift be treated as acceptable smoke-risk for now
   or diagnosed further before coupled testing?
3. If coupled smoke is approved, what minimal step schedule should be used to
   distinguish `broad/background`, `notch-amplified`, and boundary-dominated
   branches without starting an expensive full run?

