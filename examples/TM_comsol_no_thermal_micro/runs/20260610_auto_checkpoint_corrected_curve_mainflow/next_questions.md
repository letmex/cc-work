# Next Questions

1. Should old run folders without step checkpoints be left as unavailable diagnostics, rather than trying to reconstruct exact reactions from insufficient saved state?
2. For the next full D0020 run, should the report explicitly check that every load step has both a checkpoint and a corrected-curve row?
3. Should future plotting reports fail loudly if `stress_strain_primary_metric` is `reaction_metric_unavailable`?
