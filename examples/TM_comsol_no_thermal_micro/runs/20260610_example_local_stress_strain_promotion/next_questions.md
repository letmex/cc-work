# Next questions

1. Is the example-local plotting flow promotion sufficient as the main-path fix, given that the physical model and D0020 exact-reaction data are unchanged?
2. Should the next task add a small regression test for corrected CSV priority in `plot_clean_tm_results.py`, or is the current smoke evidence enough?
3. Should older figure packages be regenerated with this plotting flow, or only future packages?

