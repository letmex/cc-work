# Next Questions

1. Is this single-route interface strict enough, or should advanced `--top-u-mode` and `--coord-normalization` overrides also be moved out of the normal CLI?
2. Should the next Codex task run a short smoke training using the simplified route, or should it first inspect the source snapshots for accidental loss of required diagnostics?
3. Are the training summary columns without top-boundary reaction sufficient, or should exact energy reaction be injected into summaries only after postprocess succeeds?
