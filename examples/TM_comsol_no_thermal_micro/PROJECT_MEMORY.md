# Project Memory

Canonical next-window memory:

`examples/TM_comsol_no_thermal_micro/runs/CODEX_PROJECT_MEMORY_FOR_NEXT_WINDOW.md`

Use that file as the authoritative handoff for continuing `TM_comsol_no_thermal_micro` in a new Codex window. It records the current verified route, reaction policy, shear run history, persistent constraints, and the recommended next task.
